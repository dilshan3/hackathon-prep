{
    "version": 2,
    "functions": {
      "api/index.js": { "runtime": "nodejs18.x" }
    },
    "routes": [
      { "src": "/(.*)", "dest": "/api/index.js" }
    ],
    "buildCommand": "npm run build && npm run db:generate",
    "env": {
      "NODE_ENV": "production"
    },
    "regions": ["iad1"]
  }
  